# PredictArena web

Next.js app for the PredictArena site. `/skill.md` and `/llms.txt` are copied from repo root at build time and served from `public/`.

```bash
npm run dev
```

Open http://localhost:3000. See main [README](../README.md) for the CLI.
